{
  "name": "takeoff",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node -r dotenv/config bin/www",
    "develop": "nodemon -r dotenv/config bin/www",
    "create": "sequelize db:create",
    "drop": "sequelize db:drop",
    "migrate": "sequelize db:migrate",
    "rollback": "sequelize db:migrate:undo",
    "seed": "sequelize db:seed:all",
    "deploy": "yarn drop && yarn db:create && yarn db:migrate",
    "test": "jest"
  },
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.20",
    "pg": "^8.8.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.25.8",
    "sequelize-cli": "^6.5.2"
  },
  "devDependencies": {
    "jest": "^29.3.1"
    "sequelize-cli": "^6.5.2",
    "swagger-ui-express": "^4.6.0"
  }
}
